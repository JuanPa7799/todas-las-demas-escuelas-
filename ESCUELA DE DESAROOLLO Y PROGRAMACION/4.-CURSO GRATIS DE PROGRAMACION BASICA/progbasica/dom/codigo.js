var ruta = window.location; //No esta en parentesis por que no es una funcion, es una variable interna del objeto window.
//cuando una variable es parte de un objeto, se le llama "atributo o propiedad". location es un atributo o propiedad del window.
console.log(ruta); //Nos ayuda a ver todo desde su origen y su desglosamiento.
document.write("Estás en: " + ruta); //document es parte del objet model, es un objeto con todo el archivo, por lo que .write, invoca donde escribir en el documento.
  //cuando las funciones son parte de un objeto se llaman "metodos" (parte de un objeto).
